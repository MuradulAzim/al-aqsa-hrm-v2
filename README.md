# Al-Aqsa Security Services

Business Management Web Application

## Step 1: Foundation

This is the minimal application foundation. The app loads cleanly with no business logic.

### Current State

- Static login page (no authentication)
- Dashboard with header only (app name + version)
- No business modules implemented

### File Structure

```
├── index.html          # Login page (static UI)
├── dashboard.html      # Dashboard with header only
├── js/
│   ├── config.js       # Application constants
│   ├── utils.js        # Utility helpers
│   └── api.js          # Generic API layer (mocked)
└── README.md           # This file
```

### Tech Stack

- HTML5
- Vanilla JavaScript
- Tailwind CSS (CDN)
- Google Apps Script backend (not connected yet)

### Version

1.0.0
